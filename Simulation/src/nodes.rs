use crate::mapping::Mapping;
extern crate algo;
use algo::WeightUnit;
pub struct Coordinator{
    mapping:Vec<Mapping>,
    //todo
}
pub struct Worker{
    weights: Vec<WeightUnit>,
    inputs: Vec<f64>,
    //todo
}

impl Coordinator{
    //todo
}
impl Worker{
    //todo
}